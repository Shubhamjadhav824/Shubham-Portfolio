# Shubham Jadhav — Portfolio (React + Tailwind)

## Run locally
npm install
npm run dev

## Build
npm run build

## Deploy on Vercel
vercel

Notes:
- Resume link currently points to "#resume" — wire it to a real
  /assets/resume.pdf once you add the file.
- Profile photo is a placeholder monogram ("SJ") — swap the gradient
  div in the About section for a real <img> once you have one.
- Contact form opens the visitor's mail client on submit (no backend).
  Swap handleSubmit for a fetch() to Formspree/your own API when ready.
- Skill icons load from the public devicon CDN — no local assets needed.
